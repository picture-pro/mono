use base_components::{Section, Title};
use leptos::prelude::*;

#[component]
pub fn PhotoGroupPage() -> impl IntoView {
  view! {
    <Section>
      <Title>"Photo Group"</Title>
    </Section>
  }
}
